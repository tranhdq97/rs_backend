export enum ERouter {
  SIGNUP = "/signup",
  SIGNIN = "/signin",
  HOME = "/",
  SETTING = "/setting",
  TABLES = "/tables",
  TABLE = "/table/:index",
}

export enum ERouterName {
  SIGNUP = "signup",
  SIGNIN = "signin",
  HOME = "home",
  SETTING = "setting",
  TABLES = "tables",
  TABLE = "table",
}
